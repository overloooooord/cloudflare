curl-cffi>=0.7.0
aiohttp>=3.9.0
aiofiles>=23.2.1
playwright>=1.57.0

# Установка:
# pip install -r requirements.txt
# playwright install chromium
#
# Капча-сервис (выбери один):
#   capsolver.com  — зарегись, получи API key, вставь в config.py
#   2captcha.com   — то же самое
